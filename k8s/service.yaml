apiVersion: v1
kind: Service
metadata:
  name: clo835-assignment2-srsivagnanasundaram
spec:
  type: NodePort
  selector:
    app: clo835-assignment2-srsivagnanasundaram
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
